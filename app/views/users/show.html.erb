  <%= turbo_stream_from "inbox_list" %>
<div>
  <div class="mb-8">
    <h1 class="font-bold text-4xl">Users#show</h1>
    <p>Find me in app/views/users/index.html.erb</p>
  </div>
  <div>
    <p class="text-xl mb-2">User:  <%= @user.email %> </p>
    <p class="text-xl mb-2">ID:  <%= @user.id %> </p>
  <h2 class="font-semibold text-2xl mb-2"> Events: </h2>
  </div>

  <div id="test">
    <% @user.events.each do |event| %>
      <%= render partial: "listeventforuser", locals: { event: event}  %>
    <% end %>
  </div>
</div>
